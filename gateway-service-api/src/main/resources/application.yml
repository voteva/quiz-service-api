server:
  address: 0.0.0.0
  port: 8080
  servlet:
    context-path: /gateway-service-api
server.error.whitelabel.enabled: false

spring:
  application:
    name: gateway-service-api
  mvc:
    throw-exception-if-no-handler-found: true
  thymeleaf:
    cache: false
    mode: HTML

gateway:
  grpc:








